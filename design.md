1. make sure the navbar is always the same with the index.html file
2. make sure the footer is always the same with the index.html file
3. make sure the page is mobile responsive
4. make sure to use the same color scheme as the databank.html file
5. make sure the page is aligned and a section image or design is not flowing into another section.
6. make sure to use the same fonts as the databank.html file
7. make sure to use the same spacing as the databank.html file
8. make sure to use the same border-radius as the databank.html file
9. make sure to use the same box-shadow as the databank.html file
10. make sure to use the same transition as the databank.html file
11. make sure to use the same animation as the databank.html file
12. make sure to use the same transform as the databank.html file
13. make sure to fit the content to the screen size
14. make sure the logo is green on white or light background and white on dark or green background
15. the logo should use position: absolute (scrolls with the page content, not fixed). It should sit at the top of the page but scroll away as the user scrolls down — just like the SPV page.
16. the pill navbar should use position: fixed (always sticky at the top center of the screen). It should remain visible as the user scrolls — just like the SPV page.